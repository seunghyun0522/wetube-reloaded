header
    a(href="/").header__logo
        i.fab.fa-youtube 
    nav 
        ul 
            li 
                a(href="/search")
                    i.fas.fa-search
            if loggedIn
                li
                    a(href="/videos/upload") Upload Video
                li
                    a(href="/users/edit") Edit Profile
                li
                    a(href="/users/logout") Log Out
                li 
                    a(href=`/users/${loggedInUser._id}`)
                        if !loggedInUser.avatarUrl
                            span 😃
                        else
                            if loggedInUser.socialOnly
                                img.header__avatar(src="http://localhost:4000/" + loggedInUser.avatarUrl, crossorigin)
                            else
                                img.header__avatar(src="http://localhost:4000/" + loggedInUser.avatarUrl, crossorigin)

            else
                li 
                    a(href="/login") Log In
                li 
                    a(href="/join").header__btn Join